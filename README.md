# ThermalLabeler

**ThermalLabeler** is an Android application for printing to thermal printers without relying on manufacturer-specific drivers.

<p align="center">
  <img src="images/roll.jpg" alt="Receipt roll" width="300">
</p>

Important: the application is intended exclusively for use with thermal printers that support <b>TSPL</b> and/or <b>ESC/POS</b>:
<ul>
  <li><b>TSPL</b> - used for printing self-adhesive labels
  <li><b>ESC/POS</b> - used for printing on thermal receipt paper (receipts, invoices, tickets, and other documents)
</ul>
The application is not intended for conventional office, inkjet, laser, or other types of printers and will not work with them. Only thermal printers with a compatible <b>TSPL</b> and/or <b>ESC/POS</b> printing interface are supported.
<BR>
<BR>The application acts as a bridge between Android and a thermal printer, providing full control over printing labels and documents on thermal receipt paper where standard Android printing solutions are unavailable or create limitations. It solves a practical problem: how to print a label or a document from a phone or tablet to a compatible thermal printer.
<BR>
<BR>Before printing, the file contents are automatically converted into a raster image.
<BR>When printing labels, the raster image is scaled to the actual label size and prepared according to the selected orientation, margins, and printer settings. When printing on a receipt printer, the resulting raster image is scaled to the paper width.

---

## Label Templates

The application allows you to create and save multiple label templates with different sizes and print settings.

Each template contains a set of properties:

- label width and height  
- gap between labels  
- offset  
- orientation and rotation  
- alignment parameters  
- preprint (non-printable area)  

Saved templates can be reused and allow quick switching between different label types without reconfiguration.

---

## Direct Printing

Printing is performed directly:

- via Bluetooth  
- via USB  
- via Wi-Fi  

The application supports opening files (PDF, HTML, images) directly from the Android system.

When selecting “Open with” or “Share”, the file is automatically loaded into the application and prepared for label printing.

---

## Android PrintService Integration

The application works as an Android PrintService:

- available from the standard “Print” dialog  
- can be used by any application (POS software, browser, PDF viewer, etc.)

---

## Supported File Formats

The application allows opening and printing labels from the following file types:

- **PDF** — documents and layouts generated by other applications or systems  
- **HTML** — pages and templates, including automatically generated reports and price tags  
- **Images** — PNG, JPG, and other common formats  

---

## Print History

The application stores the history of completed print jobs.

For each job, the following are recorded:

- the original file  
- print parameters  

From the history, you can reopen a print job and perform reprinting without selecting the file again or reconfiguring label settings.

---

## How It Differs from “Typical” Solutions

- no A4 emulation  
- not tied to a single printer brand  
- precise label-to-label printing  
- full control over the entire print pipeline  
- suitable for warehouses, retail, logistics, and labeling  

---

## Typical Use Cases

- printing price tags and barcodes  
- printing warehouse and shipping labels
- Printing receipts and other documents on receipt paper
- printing from POS systems  
- printing from custom Android applications  
